function reverseString(text){
    // for (let i=text.length; i>= 0;  i--) ulta reverse
    for (let i =0; i<text.length ; i++)
    {
        const element =text[i];
        console.log(element);

    }
}
const myString='I am a bad boy';
const reversed=reverseString(myString);